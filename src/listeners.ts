import CommandListener from "./listeners/CommandListener";
import EmojiChangeListener from "./listeners/EmojiChangeListener";
import PhraseListener from "./listeners/PhraseListener";

export default [CommandListener, PhraseListener, EmojiChangeListener];
